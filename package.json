{
  "name": "batch-deletion-node-version",
  "version": "1.0.3",
  "description": "Delete files in one go with same extension",
  "main": "src/batch_deletion.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node",
    "batch-deletion",
    "cli"
  ],
  "bin": {
    "batch_delete": "src/batch_deletion.js"
  },
  "author": "udhaybegyall",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/udhaybegyall/batch-deletion-node-version.git"
  },
  "homepage": "https://github.com/udhaybegyall/batch-deletion-node-version",
  "dependencies": {
    "argparse": "^2.0.1"
  }
}
